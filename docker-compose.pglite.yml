services:
  auto-cal:
    build: .
    ports:
      - "3001:3001"
    volumes:
      - auto-cal-data:/app/pgdata
    environment:
      - NODE_ENV=production
      - PGLITE_DATA_DIR=/app/pgdata

volumes:
  auto-cal-data:
