{
  "name": "auto-cal-site",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Landing / self-hosting site for Auto Cal (built with Eleventy, hosted on GitHub Pages)",
  "scripts": {
    "dev": "eleventy --serve",
    "build": "eleventy",
    "clean": "rm -rf _site"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0"
  }
}
